# Claude Code Prompt — Use Existing Image Components

You already have image components in `assets_image_components/`.

Do not replace them with random generated images. Use these existing files as the page assets.

Primary files to use:

- `backgrounds_hero_black_hole_wide.png` as the landing hero background
- `sprites_pixel_astronaut.png` or `sprites_pixel_astronaut_small.png` as the small astronaut
- `thumbnails_black_hole_renderer.png` for the first project card
- `thumbnails_deep_field_cataloger.png` for the second card
- `overlays_code_left.png` and `overlays_code_right.png` as faint code overlays
- `ui_panel_frame.png` / `ui_telemetry_panel.png` for terminal-style side panels if needed
- SVG icons in the same folder for contact/tools UI

Recreate the reference page `reference_landing_page.jpeg` as closely as possible.

Important visual target:
- dark sci-fi terminal landing page
- centered header title
- two-line navigation
- black hole hero area
- central glowing circular logo
- tiny pixel astronaut near bottom center
- translucent project cards below
- faint code overlays
- scanlines, grain, vignette

Keep everything monochrome blue-gray. Avoid modern SaaS style.
